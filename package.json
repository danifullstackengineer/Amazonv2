{
  "name": "amazonv2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run-client": "cd client && npm start && cd ..",
    "run-server": "cd server && npm start && cd ..",
    "start": "concurrently \"npm run run-client\" \"npm run run-server\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
